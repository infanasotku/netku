services:
  app-backend:
    build:
      context: src

    volumes:
      - cert-dir:/etc/cert:ro
      - xray-dir:/etc/xray

    env_file:
      - .env

volumes:
  cert-dir:
    external: true
  xray-dir:
    external: true
