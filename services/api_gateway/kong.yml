_format_version: "3.0"
_transform: true

services:
  - name: users
    url: ${USERS_URL}/api/users
    routes:
      - name: users-crud
        methods:
          - GET
          - POST
          - PATCH
        paths:
          - /api/users
